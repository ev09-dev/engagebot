[build]
command = "npm install && npm run build"

[deploy]
startCommand = "npm start"

[env]
NODE_ENV = "production"
NEXT_PUBLIC_APP_URL = "${RAILWAY_PUBLIC_DOMAIN}"
PORT = "3000"

# Health check
[healthChecks]
path = "/api/health"
interval = "30s"
timeout = "10s"
retries = 3